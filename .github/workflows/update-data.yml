name: Update data.json (FAPEAM) - MANUAL ONLY

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Stop here (manual mode)
        run: |
          echo "Automação desativada. O site agora é atualizado manualmente (modo estável)."
